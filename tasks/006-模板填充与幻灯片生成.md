# 任务 006：模板填充与幻灯片生成

## 目标

根据模板包和表单 JSON 生成新的 PPT，首版优先支持 text/image 字段。

## 主要工作

- 定义表单 JSON schema
- 实现文本替换与图片替换
- 支持按表单顺序生成多页幻灯片
- 处理基本校验，如缺失字段、类型不匹配
- 支持在当前会话内复用已打开的模板或目标 PPT 上下文

## 交付物

- `template fill` 命令

## 验收标准

- 能依据模板包和表单输出新的 PPT
- 文本与图片字段能正确写入目标页
- 输入不合法时能返回明确错误信息

